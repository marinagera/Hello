(() => {
  function slideshow(wrapper) {
    const control = wrapper.querySelector(".slideshow__control");
    const slidesWrapper = wrapper.querySelector(".slideshow_slides");
    constslides = wrapper.querySelectorAll(".slideshow_slide");
    const transformCount = 100 / slides.length;
    let activeSlideIndex = 0;

    control.add;
  }
})();
